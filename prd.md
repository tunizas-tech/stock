# 주식 공부 노트 — PRD

> Product Requirements Document
> 버전 1.0 · 최종 수정 2026-05-31

---

## 1. 한 줄 요약

한국·미국 주식을 **사고파는 도구가 아니라, 기록하고 복기하며 배우는** 개인용 공부 워크스페이스.

## 2. 배경과 문제 정의

투자 공부를 하다 보면 정보는 흩어지고 결정의 맥락은 사라진다.

- 왜 샀는지, 살 때 어떤 심리였는지를 기록하지 않아 같은 실수를 반복한다.
- 관심 종목 메모, 보유 현황, 매매 이유가 메신저·노트·증권사 앱에 흩어져 있다.
- 시중 앱은 대부분 **실시간 매매**에 최적화돼 있어, 차분히 **복기**하는 데는 맞지 않는다.

이 앱은 "거래"가 아니라 "학습 루프(결정 → 기록 → 복기 → 패턴 발견)"를 돕는다.

## 3. 핵심 설계 원칙

| 원칙 | 의미 | 결과 |
|------|------|------|
| **복기 우선** | 실시간 트레이딩 도구가 아니다 | 종가·지연 데이터로 충분 → 무료 API로 해결 |
| **단일 사용자** | 본인 전용 (멀티유저는 나중) | 인증 복잡도 제거, localStorage로도 동작 |
| **마찰 최소화** | 기록이 귀찮으면 안 쓴다 | 가격·수량은 선택 입력, 메모만으로도 기록 가능 |
| **확장 가능한 뼈대** | 데이터 소스를 나중에 갈아끼움 | 시세 어댑터 1곳만 교체하면 실데이터 전환 |

## 4. 대상 사용자

한국·미국 주식을 함께 보는 개인 투자자(본인). 종목 분석·기록·복기를 직접 하며, 자신의 투자 판단 패턴을 데이터로 되돌아보고 싶어 한다.

## 5. 목표 / 비목표

**목표 (Goals)**
- 매매·관심 결정과 그 이유·심리를 한 곳에 누적한다.
- 보유 종목의 평가손익을 한·미 통화로 즉시 확인한다.
- 지표 기반으로 종목을 거른다.
- 시간이 지나 결정을 복기하고 패턴(예: 확신도와 실제 수익의 관계)을 발견한다.

**비목표 (Non-Goals)**
- 실시간 호가/체결, 자동 주문, 알고리즘 트레이딩.
- 타인과 공유하는 소셜 기능, 랭킹.
- 세무·정산·투자자문(법적 권유) 기능.

## 6. 모듈별 요구사항

### 6.1 매매일지 (핵심)

학습 루프의 중심. 결정과 그 맥락을 남기고 나중에 복기한다.

- 기록 항목: 날짜, 시장(KR/US), 종목코드·종목명, 구분(매수/매도/메모), 가격(선택), 수량(선택), **이유**, **당시 확신도(1~5)**, **복기(나중에 채움)**.
- 최신순 정렬, 카드형 목록, 삭제 가능.
- 복기 문장은 시각적으로 분리(인용선)해 "당시 생각"과 "지나고 나서의 깨달음"을 구분.
- *(예정)* 종목별 묶어보기, 확신도 vs 실현손익 상관, 월별 회고.

### 6.2 포트폴리오

- 보유 종목: 시장, 티커, 종목명, 수량, 평단가, 진입일.
- 시세 어댑터로 현재가를 받아 **평가금액 / 손익 / 손익률** 자동 계산(통화별 표기).
- 관심 종목: 티커 + 메모(진입 조건·관찰 포인트), 등락률 표시.
- *(예정)* 통화 환산 합계, 비중 차트, 진입가 대비 알림.

### 6.3 스크리너

- 조건 필터: 시가총액, PER, PBR, 배당수익률, 매출 성장률, 52주 고가 대비 등.
- 한·미 종목 동시 필터링, 결과 테이블 → 클릭 시 상세/관심 추가.
- **펀더멘털 데이터 소스 연동(모듈 A) 후 동작.** 현재는 필터 UI 뼈대만 존재.

## 7. 데이터 소스 전략

공부용이라 **실시간이 필요 없다**는 점이 핵심 — 무료 티어로 전부 커버된다.

| 시장 | 1순위 | 대안 | 비고 |
|------|-------|------|------|
| 한국 | 한국투자증권 **KIS Developers** | — | 무료, 국내+해외 모두 제공, REST |
| 미국 | KIS 해외주식 API (통합) | **Finnhub** (무료 60 calls/분) | 통합이면 인증 1번으로 끝 |

- **Alpha Vantage 제외** — 무료 하루 25요청으로 비현실적.
- **yfinance/Yahoo 제외** — 비공식 스크래핑이라 자주 깨짐.
- 키(APP_KEY/SECRET, FINNHUB_API_KEY)는 **서버 전용**. `/api/quotes` 라우트로 감싸고 클라이언트에 노출 금지.
- 호출 절약을 위해 `price_cache`(일자별 종가) 캐시 테이블 도입 예정.

## 8. 데이터 모델

```
holdings    (id, market[KR/US], ticker, name, shares, avgPrice, openedAt)
watchlist   (id, market, ticker, name, memo, addedAt)
journal     (id, date, market, ticker, name, action[buy/sell/note],
             price?, qty?, reason, emotion[1-5], lesson)
price_cache (ticker, market, date, close, ...)   -- 예정: 호출 절감 + 복기
```

저장은 이중 모드: 환경변수(Supabase)가 있으면 Postgres, 없으면 브라우저 localStorage. 페이지는 단일 데이터 API(`db.*`)만 호출한다.

## 9. 기술 스택

- **프론트/풀스택**: Next.js 14 (App Router), TypeScript
- **스타일**: Tailwind CSS, CSS 변수 기반 토큰
- **차트**: recharts
- **저장소**: Supabase (Postgres + Auth) — 선택, 미설정 시 localStorage 폴백
- **배포**: Vercel
- **데이터 API(모듈 A)**: KIS Developers / Finnhub (서버 라우트 프록시)

## 10. 로드맵

| 단계 | 범위 | 상태 |
|------|------|------|
| **B** | 프로젝트 스캐폴드: 3모듈 라우팅, 데이터 레이어, 디자인 시스템 | ✅ 완료 |
| **C** | 매매일지 모듈 완성 (CRUD, 확신도, 복기) | ✅ 완료 |
| **A** | 시세/펀더멘털 연동: KIS 토큰 발급 + `/api/quotes` | ✅ 완료 (시세 — 펀더멘털은 D에서) |
| **D** | 스크리너 필터 로직 + 결과 테이블 | ✅ 완료 (유니버스: 내장 대표 + 보유 + 관심) |
| **차트** | 지수·종목 일/주/월봉 (대시보드 + 포트폴리오 모달, KIS 기간별시세) | ✅ 완료 |
| **E** | 매매일지 분석: 확신도-손익 상관, 월별 회고 | ⏭ 다음 |
| **F** | (선택) Supabase Auth + RLS로 멀티기기/멀티유저 | 백로그 |

## 11. 성공 기준

- 매매·관심 결정을 **앱에 먼저** 기록하는 습관이 생긴다.
- 한 달에 한 번 이상 과거 일지를 복기한다.
- 확신도와 실제 결과의 패턴을 본인이 말로 설명할 수 있다.
- (정량) 주간 신규 일지 ≥ 3건, 30일 후에도 사용 지속.

## 12. 리스크

| 리스크 | 영향 | 완화 |
|--------|------|------|
| KIS 토큰/유량 제한 | 시세 조회 실패 | `price_cache` 캐싱, 일 1회 배치 갱신 |
| 무료 API 정책 변경 | 데이터 끊김 | 어댑터 추상화로 소스 교체 용이 |
| 기록 습관 미형성 | 앱 방치 | 입력 마찰 최소화, 메모만으로도 기록 허용 |
| 환율/통화 혼동 | 손익 오해 | 통화 명시 표기, 합산 시 환산 분리 |

## 13. 범위 밖 (명시)

자동 주문, 실시간 알림 푸시, 세금 계산, 백테스팅 엔진, 커뮤니티 공유. 필요 시 별도 PRD로 다룬다.
